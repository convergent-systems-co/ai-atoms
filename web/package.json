{
  "name": "@atoms/ai-atoms-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "prebuild": "mkdir -p public/exports public/schemas && cp ../exports/catalog.json public/exports/catalog.json && cp ../schemas/*.json public/schemas/",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^7.1.3",
    "marked": "^15.0.0"
  }
}
