{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://ai-atoms.com/schemas/hook-v1.json",
  "title": "ai-atoms hook v1",
  "description": "A hook atom — an event-driven runtime behavior wired into AI tool infrastructure (Claude Code, Copilot CLI, etc.).",
  "type": "object",
  "required": [
    "schema",
    "type",
    "id",
    "version",
    "name",
    "description",
    "event",
    "language",
    "trigger"
  ],
  "additionalProperties": false,
  "properties": {
    "schema": {
      "type": "string",
      "const": "https://ai-atoms.com/schemas/hook-v1.json"
    },
    "type": {
      "type": "string",
      "enum": [
        "hook"
      ]
    },
    "id": {
      "type": "string",
      "pattern": "^hook/[a-z0-9][a-z0-9-]*$",
      "description": "Namespaced identifier in the form hook/<slug>."
    },
    "version": {
      "type": "string",
      "pattern": "^[0-9]+\\.[0-9]+\\.[0-9]+(?:-[A-Za-z0-9.-]+)?$",
      "description": "Semantic version of this hook atom."
    },
    "name": {
      "type": "string",
      "minLength": 1,
      "maxLength": 80,
      "description": "Human-readable display name."
    },
    "description": {
      "type": "string",
      "minLength": 1,
      "maxLength": 1000,
      "description": "What this hook does, what event it responds to, and what it enforces or emits."
    },
    "event": {
      "type": "string",
      "description": "The AI tool event this hook responds to (e.g. PreToolUse, PostToolUse, SessionStart, Stop, UserPromptSubmit, SessionEnd, SubagentStop, PreCompact). Empty string for library-type atoms (trigger.type = library)."
    },
    "language": {
      "type": "string",
      "enum": [
        "python",
        "bash",
        "javascript",
        "typescript"
      ],
      "description": "Implementation language of the hook script."
    },
    "trigger": {
      "type": "object",
      "required": [
        "type"
      ],
      "additionalProperties": false,
      "description": "Condition under which this hook fires.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "tool-name",
            "file-pattern",
            "always",
            "tool-category",
            "library"
          ],
          "description": "How the hook matches: by tool name, file path pattern, always, tool category, or library (not a runnable hook — a shared dependency)."
        },
        "pattern": {
          "type": "string",
          "description": "Glob or regex pattern used with tool-name and file-pattern trigger types."
        }
      }
    },
    "script_path": {
      "type": "string",
      "description": "Relative path to the hook implementation script within this repository."
    },
    "blocking": {
      "type": "boolean",
      "description": "If true, the hook can abort the triggering operation. If false, it is advisory only."
    },
    "side_effects": {
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1
      },
      "uniqueItems": true,
      "description": "Observable effects of this hook (e.g. writes to audit log, emits warning to stderr)."
    },
    "platforms": {
      "type": "array",
      "items": {
        "type": "string",
        "enum": [
          "linux",
          "macos",
          "windows"
        ]
      },
      "uniqueItems": true,
      "description": "Platforms on which this hook's logic is verified. Omit to mean all platforms. Wiring is always cross-platform via 'ai hooks run <slug>'."
    },
    "platform_notes": {
      "type": "string",
      "description": "Notes about platform-specific behavior, prerequisites, or known gaps."
    },
    "authored_by": {
      "type": "string",
      "description": "Key atom ID or author identifier."
    },
    "script": {
      "type": "string",
      "description": "The complete Python (or shell) source of the hook. Installed to ~/.ai/hooks/<slug>.<ext> by the AI CLI."
    },
    "depends_on": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Hook IDs this hook depends on (e.g. hook/lib). Installed automatically.",
      "default": []
    },
    "requires_wrap": {
      "type": "object",
      "required": [
        "binary",
        "description",
        "install_hint"
      ],
      "additionalProperties": false,
      "description": "Present when the hook only achieves full coverage when the named binary is installed as a wrapper around an external tool (e.g. git aliased to 'ai git'). Without the wrapper the hook still fires for AI tool Bash calls, but direct invocations bypass it. The ai CLI reads this field in 'ai doctor' to surface the hint and in 'ai setup' to offer the wrapper install — but never requires it.",
      "properties": {
        "binary": {
          "type": "string",
          "description": "The ai CLI binary that provides the wrapper (e.g. 'ai')."
        },
        "description": {
          "type": "string",
          "description": "One sentence explaining what the wrapper does and why it is needed for full coverage."
        },
        "install_hint": {
          "type": "string",
          "description": "The CLI command the user can run to install the wrapper (e.g. 'ai setup --git-shim')."
        }
      }
    },
    "category": {
      "$ref": "https://ai-atoms.com/schemas/common-v1.json#/$defs/category"
    },
    "provenance": {
      "$ref": "https://ai-atoms.com/schemas/common-v1.json#/$defs/provenance"
    },
    "tags": {
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1,
        "maxLength": 40
      },
      "uniqueItems": true
    },
    "lifecycle": {
      "type": "string",
      "enum": [
        "draft",
        "stable",
        "deprecated"
      ],
      "description": "Lifecycle stage of this hook atom."
    }
  }
}
