# Examples

One minimal, valid atom per class, for starting a new one by hand. They are not catalog atoms: ids are `example-*`, lifecycle is draft, and the build never reads this directory. `scripts/tests/test_examples.py` validates each against its class schema so they cannot drift.
