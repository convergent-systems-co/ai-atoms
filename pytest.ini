# Two test packages (scripts/tests, atoms/hook/tests) share the basename "tests";
# importlib mode keeps them from shadowing each other on sys.path.
[pytest]
addopts = --import-mode=importlib
testpaths = scripts/tests atoms/hook/tests
